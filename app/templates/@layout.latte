<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<title>{block title|striptags|upper}Nette Application Skeleton{/block}</title>
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<meta name="description" content="">
		<meta name="author" content="">

		{* jQuery *}
		<script src="{$basePath}/jquery/js/jquery-1.8.3.js"></script>
		<script src="{$basePath}/js/netteForms.js"></script>

		{* bootstrap *}
		<link href="{$basePath}/bootstrap/css/bootstrap.css" rel="stylesheet">
		<link href="{$basePath}/bootstrap/css/bootstrap-responsive.css" rel="stylesheet">
		<script src="{$basePath}/bootstrap/js/bootstrap.min.js"></script>

		<style type="text/css">
			body {
				padding-top: 60px;
				padding-bottom: 40px;
			}
		</style>
		<script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>

		{block head}{/block}
	</head>

	<body>
		<div class="navbar navbar-inverse navbar-fixed-top">
			<div class="navbar-inner">
				<div class="container">
					<a class="brand" href="{$basePath}/">Project name</a>
					<div class="nav-collapse collapse">
						<ul class="nav">
							<li class="active"><a href="{$basePath}/">Home</a></li>
						</ul>
						{control login}
					</div>
				</div>
			</div>
		</div>

		<div class="container">

			<div class="row">
				<div class="span12">
					<div n:foreach="$flashes as $flash" class="alert{if $flash->type} alert-{$flash->type}{/if}">
						<button type="button" class="close" data-dismiss="alert">&times;</button>
						{if $flash->type}<h4>{$flash->type}</h4>{/if}
						{$flash->message}
					</div>
					{include #content}
				</div>
			</div>

			<hr>

			<footer>
				<p>&copy; Company 2012</p>
			</footer>

		</div>
	</body>
</html>
